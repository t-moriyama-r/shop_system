{
  "name": "kojin-pos",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev:front": "pnpm --filter front dev",
    "dev:backend": "pnpm --filter backend dev",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:generate": "pnpm --filter db generate",
    "db:migrate": "pnpm --filter db migrate",
    "db:seed": "pnpm --filter db seed",
    "db:cleanup-sessions": "pnpm --filter db cleanup-sessions",
    "db:cleanup-audit-logs": "pnpm --filter db cleanup-audit-logs",
    "db:create-se-admin": "pnpm --filter db create-se-admin",
    "db:delete-se-admin": "pnpm --filter db delete-se-admin",
    "test": "pnpm -r test"
  }
}
